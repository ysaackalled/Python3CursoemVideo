import math
ang = float(input('Digite um angulo: '))
seno = math.sin(ang)
cosseno = math.cos(ang)
tangente = math.tan(ang)
print('Seno de {} é {:.2f}\nCosseno de {} é {:.2f}\nTangente de {} é {:.2f}'.format(ang, seno, ang, cosseno, ang, tangente))
